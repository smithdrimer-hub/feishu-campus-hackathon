# OpenClaw Memory Engine — Demo 演示剧本

## 30 秒价值陈述

> "飞书群里讨论的内容——谁负责什么、决定了什么、被什么阻塞了——通常散落在历史消息里。下次开会时没人记得上次的决定。Memory Engine 自动从群聊中提取结构化协作状态，生成带证据锚点的交接摘要。它不是聊天摘要，是可审计的协作记忆。"

## 场景设定

一个开发团队在飞书群讨论 V1.6 优化任务：

| 角色 | 身份 | 行为 |
|------|------|------|
| 张三 | 后端开发 | 发布目标、分配任务、报告进展 |
| 李四 | 前端开发 | 接受任务、报告阻塞 |
| 王五 | 项目经理 | 做决策、调整优先级 |

## 输入消息（评委可见）

```
张三: 目标：完成 V1.6 综合优化
张三: 负责人：张三负责测试模块
王五: 决策：先修复否定极性问题
李四: 阻塞：测试数据还没准备好
张三: 我这周请假，有事找李四
王五: 改为：先做文档，测试延后
```

## 一键运行

```bash
cd openclaw-memory
python scripts/demo_run_example.py
```

## 输出展示（评委可见）

Memory Engine 提取出以下结构化记忆：

### 当前项目目标
- **完成 V1.6 综合优化** — 置信度 0.65

### 当前负责人
- **张三负责测试模块** — 置信度 0.70

### 当前关键决策
- **先修复否定极性问题** — 置信度 0.75
- **先做文档，测试延后**（覆盖了上一条决策）— 置信度 0.75

### 成员状态与可用性
- **我这周请假，有事找李四** — 置信度 0.65

### 当前阻塞与风险
- **测试数据还没准备好** — 置信度 0.70

### 每条记忆都绑定证据锚点
```
来源: chat_group_01/msg_001 @ 2026-04-28T10:00:00: "目标：完成 V1.6 综合优化"
```

## 演示亮点

### 亮点 1：决策覆盖（Decision Override）
- 输入中有"决策：先修复否定极性问题"和"改为：先做文档，测试延后"
- 输出中旧决策被标记为 `superseded`，新决策为 `active`
- 历史版本保存在 `list_history()` 中

### 亮点 2：证据锚点（Source Ref）
- 每条记忆都绑定原始消息 ID 和摘要
- 可以追溯到"为什么当时这样决定"

### 亮点 3：安全边界
- 只读命令（`im +chat-search`, `docs +fetch`）自动放行
- 写入命令（`task +create`, `im +messages-send`）默认拦截
- `docs +create --dry-run` 曾经实际创建过文档，V1 明确禁止

## 可切换的展示路径

### 路径 A：快速演示（3 分钟）
1. 运行 `python scripts/demo_run_example.py`
2. 展示结构化记忆输出
3. 展示 handoff 摘要
4. 评委提问→用 `docs/judge_qna.md` 回答

### 路径 B：深入演示（5 分钟）
1. 运行 `python scripts/run_golden_eval.py` — 展示 29/30 通过
2. 展示 `golden_set.jsonl` 的 12 种场景
3. 展示 `docs/evaluation_report.md` 的指标

### 路径 C：混合搜索演示（2 分钟加分环节）

**场景设定：** 系统中已有多条记忆（来自路径 A 的提取结果），现在展示搜索能力。

**演示步骤：**

1. 先展示关键词搜索的局限：
   ```bash
   # 搜索"风险" — 关键词只能找到字面匹配
   python scripts/demo_hybrid_search.py --query "风险" --mode keyword
   # 结果：（空或极少）— 因为没有记忆里字面包含"风险"
   ```

2. 再展示混合搜索的语义理解：
   ```bash
   # 同样搜索"风险" — 向量理解语义
   python scripts/demo_hybrid_search.py --query "风险" --mode hybrid
   # 结果：找到 blocker（"测试数据还没准备好"）+ deadline（"先做文档，测试延后"）
   # 因为系统理解"风险"在项目管理中 = 阻塞 + 延期
   ```

3. 总结话术：
   > "关键词搜索找到你说的，语义搜索找到你想说的。我们两者结合：精确匹配优先，语义补充召回。"

**技术要点（评委追问时用）：**
- 使用 OpenAI text-embedding-3-small 做中文语义向量化
- ChromaDB 做本地向量存储，零运维
- Reciprocal Rank Fusion 融合两路结果，关键词权重 70%、向量权重 30%
- 没有 embedding API 时自动降级为纯关键词，不影响核心功能

## 当前限制（演示中必须诚实说明）
- 当前使用关键词规则提取，不是真实 LLM。真实场景会接入 LLM。
- 只处理群聊消息，文档/任务/会议数据源是 P1 计划。
- debounce 是安全的 coalescing，没有后台调度器。
- 不同群聊的记忆通过 project_id 软隔离，无真实权限控制。